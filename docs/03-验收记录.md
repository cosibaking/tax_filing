# MVP 验收记录

> 版本：v0.1 | 日期：2026-06

## AC 验收清单

| # | 验收项 | 状态 | 验证方式 |
|---|--------|------|---------|
| AC-01 | 匿名诊断 | 通过 | `/diagnosis` → 四列税负对比 |
| AC-02 | 注册签约 | 通过 | 注册→风险告知→签约→订单 active |
| AC-03 | OPC 落地 | 通过 | 资料提交→admin opc-tasks 更新→时间轴 |
| AC-04 | 月度台账 | 通过 | 收入/费用录入→利润表 |
| AC-05 | 申报任务 | 通过 | 申报日历→admin filing 标记 |
| AC-06 | 对账单 | 通过 | Cron/API 生成→statement 列表 |
| AC-07 | 合规留痕 | 通过 | ComplianceConsent + AuditLog |
| AC-08 | 敏感数据 | 通过 | AES 加密 + 脱敏 + Bearer 鉴权 |

## 测试命令

```bash
npm test
npm run typecheck
npm run lint
```

## 冒烟路径

```
/ → /diagnosis → /diagnosis/result
→ /user/register → /user/compliance/plan → /user/compliance/opc
→ (admin) /admin/compliance/opc-tasks
→ /user/compliance/income → expense → ledger → tax → statement
```

## 已知限制（MVP Out of Scope）

- 无真实电子税务局 API
- 无腾讯电子签（姓名确认 + PDF 存档）
- 无在线支付
- CSV 导入（无 OCR）

## 开发备忘

| 日期 | 事项 | 说明 |
|------|------|------|
| 2026-06-08 | OPC 资料真实性校验 Mock | `/user/compliance/opc` 提交注册资料时，身份证 OCR、手机实名、三要素等第三方校验暂未接入，前后端均以 Mock 放行。后端：`compliance.verifyProvider=mock`（或 env `COMPLIANCE_VERIFY_PROVIDER=mock`），逻辑见 `server/internal/library/complianceverify/`；Mock 下身份证仅校验 18 位格式、跳过校验位。前端：`VITE_COMPLIANCE_VERIFY_MOCK=true`（`.env.development` 已默认开启），表单顶部显示黄色提示条。生产接入第三方后改配置并实现 `VerifyMaterials`，前端设 `VITE_COMPLIANCE_VERIFY_MOCK=false`。 |
